---
const { src, name, location } = Astro.props;
---
<div class="card w-64 mb-4 bg-base-100 dark:bg-slate-600 shadow-xl">
  <figure class="px-5 pt-5">
    <img src={src} alt={name} height="100" class="rounded-xl grayscale transition duration-500 hover:grayscale-0 hover:scale-105" />
  </figure>
  <div class="card-body items-center text-center">
    <h2 class="card-title">{name}</h2>
    <p>{location}</p>
  </div>
</div>